include 'config'
include 'registry'
include 'gateway'
include 'user'
include 'authorization'
include 'catalog'
include 'cart'

rootProject.name = 'elibrary'

rootProject.children.each {
    it.buildFileName = "${it.projectDir.name}.gradle"
}
